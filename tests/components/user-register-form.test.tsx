import { screen } from "@testing-library/react";
import { render } from "tests/utils/render";
import { describe, expect, it } from "vitest";
import { UserCard } from "@/routes/cards/-components/user-card";

describe("名刺登録ページのテスト", () => {
  it("タイトルが表示されている", () => {});
});
